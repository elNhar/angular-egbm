export interface CardSection {
    fields: {
        title?: string;
        subtitle?: string;
        description: string;
        image: any;
        viewMoreLink?: string;
        linkedIn?: string;
    };
}
